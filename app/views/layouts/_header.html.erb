<header class="navbar navbar-white d-flex flex-column flex-md-row align-items-center p-3 px-md-5 mb-3 bg-body border-bottom shadow-sm">
  
  <nav class="my-0 d-flex">
  	<h2 class="my-0 home-link"><%= link_to 'TripSearch', root_path %></h2>
    <a class="p-2 ml-5" href="#">Контакты</a>
    <a class="p-2 ml-5" href="#">Создать маршрут</a>
  </nav>
  <% if user_signed_in? %>
  	<a class="p-2"><%= link_to "Выйти", destroy_user_session_path, :method => :delete %></a>
  <% else %>
  	<a class="p-2"><%= link_to "Войти", new_user_session_path %></a>
  <% end %>
</header>
